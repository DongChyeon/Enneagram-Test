## enneagram-test-site - 2026-09-20

> 상태 갱신: **Step 1 완료 반영** (2026-09-20). 미해결 질문 **1건이 추가 종결**되어, 원 5건은 **전건 종결**됐다.
> 남은 항목은 "질문"이 아니라 **실행 중 확정해야 할 blocking 입력값** 2건뿐이다. 계획 §11 참조.
>
> 이전 갱신: **consensus iteration 3 + final improvements** (2026-09-20). 5건 중 **4건 종결, 1건 유지**.
> (iteration 2에서 3건 종결 → iteration 3에서 "PNG 카드 세로 비율"이 freeze로 추가 종결.)
> 개선 패스에서는 종결 상태 변화 없이 **"프로덕션 도메인" 선행 조건의 진술만 정정**했다 — 플레이스홀더를 그대로 두면 빌드가 실패하므로, 항목의 성격이 "값 미정"이 아니라 **"유효한 잠정 상수로 시작해 배포 직전에 교체"** 로 바뀌었다. 계획 §10-1 참조.

### 종결 (closed)

- [x] **자체 작성 문항의 리뷰 주체** — **종결**. 결정: 작성 패스와 리뷰 패스를 **별도 에이전트 컨텍스트**로 분리한다. 리뷰어에게는 문항 텍스트 + `typeId` + 이론 출처만 전달하고 **authoring transcript는 전달하지 않는다**. 리뷰어 식별자가 `Question.copyrightReview.reviewedBy`에 기록되며 **같은 문항의 `Question.authoredBy`와 같으면 AC-14가 실패한다**. (iteration 3: 비교 대상이 정의되지 않은 전역 상수 `AUTHOR_ID`였던 것을 **문항별 `authoredBy` 필드**로 교체 — 9배치 작성이므로 작성자는 문항마다 다를 수 있다.) → 계획 §5 Step 2.5, AC-2, AC-14
- [x] **1·2위 점수차가 작을 때의 "유형 불명확" 안내 임계값** — **종결**. 결정: **절대 점수차 `1위 − 2위 < 3`** (10~50 척도). `lib/scoring.ts`의 `isAmbiguous(scores): boolean`로 구현하고 단위 테스트(차이 2 → true, 3 → false)로 고정. 결과 화면은 원점수 단독이 아니라 **상대 비율 막대 + 1–2위 점수차**를 함께 표시한다(전 문항 정채점이라 묵종 응답자의 절대 점수가 밀집하므로 원점수는 절대 규준처럼 오독된다). (iteration 3: 이 안내는 AC-8 ⑤이며, V5 렌더 체크리스트가 이를 **누락**하고 있었다 — **임계값 양쪽 픽스처**로 노출/미노출을 둘 다 검사하도록 복원했다.) → 계획 AC-8 ⑤, Step 3.2, §7 V5
- [x] **한글 서브셋 폰트 선정** — **종결**. 결정: **Pretendard (OFL)**. 서브셋 글리프 집합은 고정 KS X 1001이 아니라 `data/types.ts` + `data/wings.ts` + 카드 고정 문구의 **유니크 문자 ∪ 숫자 ∪ 라틴 기본**을 데이터에서 계산해 `pyftsubset`에 넘긴다. 형식은 `.ttf`(satori는 woff2 미지원). 파일 위치는 `assets/fonts/`이며 Node 런타임 모듈 스코프에서 1회 로드한다. (iteration 3 보강: ① 로드 형태를 `readFileSync(new URL(..., import.meta.url))`로 고정하고 `outputFileTracingIncludes`를 병행 — `process.cwd()` 결합은 Vercel output file tracing이 따라가지 못해 배포에서만 폰트가 누락된다 ② 서브셋이 **낡는** 회귀를 잡기 위해 `scripts/check-glyphs.ts`를 신설해 V1·V3에 배선 ③ `pyftsubset`은 Python `fonttools` CLI이므로 **대역 외 선행 조건**으로 선언 ④ 라이선스 기록 위치를 `docs/sources.md`가 아닌 **`docs/licenses.md`**로 분리 — `sources.md`는 문항 출처 전용이며 AC-3의 섹션 파서 대상이다.) → 계획 ADR Consequences, Step 5.1, R13
- [x] **PNG 카드의 세로 비율(1080×1350) 확정 여부** — **종결 (closed-by-freeze, iteration 3)**. 결정: **1080×1350으로 고정(freeze)한다.** AC-11이 이 값을 명시적으로 동결하며, 인스타 스토리(1080×1920) 등으로의 변경은 **사용자 재승인 사항**이다 — 구현 편의나 디자인 선호를 이유로 구현 중에 바꿀 수 없다. 변경 시 영향 범위는 `components/ShareCardArt.tsx`와 `app/result/[code]/card/route.tsx` 2개 파일로 한정된다. → 계획 AC-11, Step 5 Rollback

- [x] **공개 라이선스 한국어 애니어그램 척도가 실제로 존재하는가** — **종결 (Step 1 완료, 2026-09-20)**. 답: **존재하지 않는다.** 한국어·영어를 통틀어 문항 전문 + 9유형 채점 키가 공개되고 재사용이 허용되는 척도가 확인되지 않았다(`docs/sources.md` §3). **IPIP는 확정적으로 부재** — 전체 문항 풀 849KB를 포함한 5개 페이지 직접 grep 결과 `enneagram` **0건**이며 공표된 IPIP→애니어그램 채점 키도 없다. 유일한 공개 후보 **OEPS**는 ①36/54문항으로 분량 미달 ②CC BY-NC-SA 추정(SA면 전체 세트 상속, NC면 수익화 차단) ③Cronbach's α ≈ 0.46 + 애호가 자기보고 준거 채점 — 세 사유로 기각했다. 따라서 **결정 C = C2 확정**이고 C1·C3는 증거로 종결됐다. 법적 근거는 *Arica Institute, Inc. v. Palmer*, 970 F.2d 1067 (2d Cir. 1992). → 계획 §2 결정 C, ADR Legal basis, §11-E2

### 미해결 (open)

**없음.** 원 미해결 질문 1건이 Step 1로 종결됐다. 아래 "실행 중 확정해야 할 선행 조건"은 질문이 아니라 값 확정 항목이다.

**추적하지 않기로 한 실마리 (기록용)**: 아랍어 45문항 애니어그램 척도(Alsoudi et al., 2025, *Social Sciences & Humanities Open*, DOI 10.1016/j.ssaho.2025.102150)는 **논문이 CC BY 4.0 gold OA**이나 45문항이 부록에 인쇄됐는지 **미확인**이다(ScienceDirect 403). 확인되더라도 아랍어→한국어 번역이고 45문항이므로 **C2 판정을 뒤집지 않는다.** 재개할 조건이 없으므로 미해결 항목으로 올리지 않는다.

### 실행 중 확정해야 할 선행 조건 (질문이 아니라 blocking 입력값)

> 미해결 질문과 달리, 아래는 "무엇을 결정할지"가 아니라 "값을 확정해 넣어야" 진행되는 항목이다.

- [ ] **프로덕션 도메인** — `app/layout.tsx`의 `metadataBase`가 **환경변수 없는 하드코딩 상수**이므로(AC-12의 `process.env` 0건 규칙과 AC-10의 OG 절대 URL 요구가 교착하는 것을 이 방식으로 해소했다), 도메인이 확정되지 않으면 AC-10이 닫히지 않는다.
  - **막히는 항목은 "값을 무엇으로 둘까"가 아니라 "언제 교체하는가"다.** Step 4.1은 **유효한 잠정 상수**(Vercel이 부여한 프리뷰 호스트명, 예: `https://enneagram-test.vercel.app`)를 넣고 시작한다 — 플레이스홀더 문자열 `'https://<production-domain>'`을 그대로 두면 `<`·`>`가 WHATWG URL의 금지 호스트 코드 포인트라 `new URL()`이 `TypeError: Invalid URL`을 던지고 **첫 `npm run build`가 통째로 실패해 OG PNG가 0개 생성된다**. (iteration 3 개선 패스에서 Architect·Critic이 공통으로 지적.)
  - **Step 6의 blocking 선행 조건**: `vercel --prod` **전에** 잠정 상수를 확정된 프로덕션 도메인으로 **교체**하고 `npm run build`를 다시 돌려 OG PNG를 재생성한다. 이후 도메인이 또 바뀌면 같은 교체 + 재배포가 필요하다. → 계획 ADR Consequences, Step 4.1, Step 6 선행 조건, §10-1
- [ ] **배치별 작성자 식별자** — `Question.authoredBy`에 들어갈 값. Step 2의 9배치 각각에 대해 확정되어야 하며, Step 2.5 리뷰어 식별자와 겹치면 AC-14가 실패한다. → 계획 Step 2 배치 전략, AC-14
- [ ] **`pyftsubset`(Python fonttools) 설치** — 프로젝트 의존성이 아닌 **작성자 로컬 환경 요건**. Step 5.1 착수 전 `pyftsubset --help` 동작 확인. 산출물(`assets/fonts/*.ttf`)만 커밋되므로 Vercel 빌드 환경에는 Python이 필요 없다. → 계획 Step 5.1

### Step 2 착수 시 확정해야 할 항목 (Step 1 완료로 새로 생긴 것)

- [ ] **역채점 문항의 유형별 개수 (1개인가 2개인가)** — AC-1은 `1 이상 2 이하`만 강제한다. 유형마다 다르게 둘 수 있으나, **9유형 전부 동일하게 두는 편이 유형 간 점수 분포의 비교 가능성을 해치지 않는다**(한 유형만 역채점 2개면 그 유형의 묵종 방어 강도가 달라진다). 배치 1 작성 시 결정하고 나머지 8배치에 일관 적용한다. → 계획 Step 2 규칙 5-a, AC-1
- [ ] **`이론적 해석` 마커의 정확한 표기** — AC-8 ③과 AC-11이 요구하는 문자열. 결과 페이지와 PNG 카드가 **같은 문자열**을 써야 하며, 이 문자열은 **카드 고정 문구**의 일부이므로 폰트 서브셋 글리프 집합 계산에 들어간다(확정 후 바꾸면 `check-glyphs`가 빌드를 실패시킨다). Step 5 착수 전 확정. → 계획 AC-8 ③, AC-11, Step 5.1·5.3, §11-E4
- [ ] **면책 고지 최종 문구** — "교육·자기이해 목적 / 임상적 진단 아님" + **"요인분석이 9개보다 적은 요인을 산출한다"** 는 연구 결과 문장. 초안은 `docs/sources.md` §5에 있으므로 **새로 짓지 말고 그것을 다듬는다**. 랜딩·결과 페이지·PNG 카드 3곳에 같은 문구가 들어간다(R3 완화 ①). → 계획 AC-8 ⑥, AC-11, R3, §11-E4

---

## 실행 착수 시 확정값 (2026-09-20 확정)

- [x] **유형당 역채점 문항 수 = 2 (9개 유형 전부 동일, 총 18/90)**
  근거: 유형마다 역채점 수가 다르면 묵종 보정의 강도가 유형별로 달라져, 점수 분포에 문항 설계에서
  비롯된 인공적 편차가 생긴다. 균일하게 두면 그 교란이 사라진다. `data/questions.test.ts`는
  "유형당 정확히 2개"를 단언한다(범위 1~2가 아니라 고정값 2).

- [x] **"이론적 해석" 표시 문구 확정** — 두 문자열 모두 폰트 서브셋 글리프 집합에 포함되며,
  Step 5 이후 변경 시 `scripts/check-glyphs.ts`가 실패하고 OG 이미지 18종을 재생성해야 한다.
  - 결과 페이지 (윙 블록에 시각적으로 인접): `윙은 이론적 해석이며 검증된 측정 결과가 아닙니다.`
  - PNG 카드 (공간 제약): `윙 — 이론적 해석`

- [x] **최종 면책 문구 확정** — 결과 페이지·랜딩·카드 3면에 노출. 카드는 마지막 한 줄만 사용.
  ```
  이 검사는 에니어그램 이론에 근거해 자체 제작한 90문항 자기보고 도구입니다.
  상용 에니어그램 검사의 문항을 사용하지 않았으며, 표준화·타당화 절차를 거치지 않았습니다.
  결과는 진단이 아니라, 아홉 가지 동기 패턴에 대해 당신이 스스로를 어떻게 보고했는지를
  정리한 것입니다. 점수가 비슷한 유형이 여럿일 수 있고, 시기와 상황에 따라 결과가 달라질 수 있습니다.
  에니어그램은 학술적 검증 근거가 제한적이며, 요인분석 연구들은 아홉 개의 독립된 차원을
  일관되게 확인하지 못했습니다 (Hook et al., 2021).
  채용·인사 평가·진로 결정·정신건강 판단의 근거로 사용하지 마십시오.

  교육·자기이해 목적이며 임상적 진단이 아닙니다.
  ```
